1.  bikin topologi tree dengan root Lain dan sisa nya leaf
   ```
    auto eth0
	iface eth0 inet static
	    address 192.226.a.b
	    netmask 255.255.255.0
	    gateway 192.226.a.c
   ```
- interface leaf di eth0
- a: subnet nya menyesuaikan gateway
- c: id nya Lain / root nya (1)
- b: id nya leaf, bisa berapa aja dari 2-254 (lupa 254/255)
```
auto eth0
iface eth0 inet dhcp
    up sysctl -w net.ipv4.ip_forward=1
    up iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE

auto ethn
iface eth1 inet static
    address 192.226.1.1
    netmask 255.255.255.0
```
- eth0 root dibikin dhcp ke NAT
- ethn menyesuaikan interface nya root, dan dibikin statis
- sysctl blablabla biar bisa forward ipv4?
- iptables yang ini biar paket yang keluar ke NAT diganti dari ip vpc jadi ip lokal -> 10.4.89.247 jadi 192.1668.x.x

2. pasang iptable di lain buat forward paket dari interface eth{1,2,3} ke eth0, terus di masing masing leaf dikasih tau nameserver 8.8.8.8
```
iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE 
iptables -A FORWARD -i eth1 -o eth0 -j ACCEPT 
iptables -A FORWARD -i eth2 -o eth0 -j ACCEPT
iptables -A FORWARD -i eth3 -o eth0 -j ACCEPT 
iptables -A FORWARD -i eth0 -m state --state ESTABLISHED,RELATED -j ACCEPT
```

3. diping aja
![gambar](3pingsesama.png)

4. ya, ini yang masukin nameserver 8.8.8.8 biar bisa connect ke NAT
![gambar](3pinggoogle.png)

5. ini aku masukin semua command ke masing masing .bashrc aja biar auto run pas start
   (tapi bisa bikin file di root, terus execute nya dari .bashrc)

6. tinggal curl
   ```
   curl -L "https://drive.usercontent.google.com/download?id=1G9zIi20ofbOgfffor-i-e7QKU3Ihe42W&export=download&confirm=t" -o nama_file.zip
   ```
- terus install unzipper -> unzip 
- ke gns -> klik kanan kabel mika -> start capture
- kebuka wireshark -> di mika run bash script nya -> selesai, ke filter ketik `dns || icmp`
[wireshark file 6](soal6.pcapng)
![[wireshark6.png]]
kalo dari ini nya sih ya, ini yang lolos kedisplay ada 88.9%, tapi yang lolos (ga ke drop) itu 100%

6. install ftp `apt update && apt install ftp acl` (ftp buat file transfer protocol, acl buat access control list)
>setup user

- useradd -M eiri && passwd eiri
- useradd -M alice && passwd alice
- useradd -M mika && passwd mika

> setup access control

   - `mkdir -p /var/wired/data`
   - `setfacl -m u:alice:rwx /var/wired/data`
   - `setfacl -m u:mika:r-x /var/wired/data`
   - `usermod -d /var/wired/data/ alice`
   - `usermod -d /var/wired/data/ mika`
   >setup ftp server pake vsftpd
   
   - apt install vsftpd
   - setup /etc/vsftd.conf nya
     ```
anonymous_enable=NO local_enable=YES write_enable=YES chroot_local_user=YES allow_writeable_chroot=YES userlist_enable=YES userlist_deny=YES userlist_file=/etc/vsftpd.user_list
     ```
     - `echo "eiri" > /etc/vsftpd.user_list`
    ```
    root@Chisa:~# getent passwd alice mika eiri
alice:x:1000:1000::/var/wired/data:/bin/sh
mika:x:1001:1001::/var/wired/data:/bin/sh
eiri:x:1002:1002::/home/eiri:/bin/sh
    ```

> start ftp server service

```
service vsftpd start
service vsftpd status
```

- `echo "Signal from Alice" > signal_alice.txt`
- ftp <IP_Chisa>
- put signal_alice.txt
pov alice:
![[ftpalice.png]]
pov chisa:
```
root@Chisa:~# ls /var/wired/data/
signal_alice.txt
```
pov mika:
![[mika_gabisa_get.png]]
karena permissionnya cuman buat /var/wired/data
- setfacl -m d:u:mika:r-x /var/wired/data/
- setfacl -m d:u:alice:rwx /var/wired/data/
- service vsftpd restart
- reupload file alice

pov mika:
![[povmika.png]]

pov eiri kena banned:
![[poveiri.png]]

8. install ftp, intall `apt install p7zip-full`
- curl -L "https://drive.usercontent.google.com/download?id=1lFepK4wFmx55PnRki3NsHW-ivudSR0vg&export=download&confirm=t" -o nama_file.zip
- 7z -x file.zip nya
>login

![[Pasted image 20260916143858.png]]
>put knights_report.txt

![[Pasted image 20260916144043.png]]
![[Pasted image 20260916144601.png]]
STOR -> start put
transfer with TCP 56180->29430
end 226 success status code

![[Pasted image 20260916145418.png]]
masuk ke EPSV (Extended Pasive Mode) 

jadi kaya client(C), server(S) tuh:
- C connect ke S via FTP
- C request put file -> minta port buat TCP tranfer file
- masuk ke port buat TCP transfer file (EPSV)
- transfer file
- keluar EPSV
- lanjut di port 21 (FTP awal)

9. curl dulu di /var/wired/data
-    curl -L "https://drive.usercontent.google.com/download?id=1tKZu0rcti4t-fXX4jtXDSKDBWzsawfoN&export=download&confirm=t" -o nama_file.zip
- install p7zip-full
- 7z x file zip nya 
![[Pasted image 20260916151529.png]]
bisa donlot dari chisa via mika
tapi,
![[Pasted image 20260916151917.png]]
gabisa upload
(error code nya 553 Could not create file.) karena servernya pake vsftpd 3.0.5 jadi errornya bukan dari permission denied, tapi server gagal membuat file pada path/nama yang diminta.

10. tes ping knight
![[Pasted image 20260916152324.png]]
nilai ICMP Type = 0 Code 0 (reply)
![[Pasted image 20260916152533.png]]
nilai ICMP Type = 8 Code = 0 (request)
![[Pasted image 20260916152704.png]]
```
77 packets transmitted, 38 received, 50.6493% packet loss, time 25669ms
rtt min/avg/max/mdev = 0.545/0.685/0.908/0.086 ms
```
packet loss nya ~50.6%
nilai terkecil rtt nya 0.545
nilai terbesar rtt nya 0.908
nilai ratarata rtt nya 0.685
nilai standar deviasi rtt nya 0.086

11. bikin user password dulu
useradd -M phantom_user
passwd phantom_user
- di chisa install telnetd
- make sure jalan `ss -lnt | grep ':23'`
> problem: ga nyala port nya
- echo 'telnet stream tcp nowait root /usr/sbin/telnetd telnetd' >> /etc/inetd.conf
- service inetutils-inetd restart

di mika:
- telnet ip nya chisa
- ![[Pasted image 20260916161003.png]]
- di wireshark -> ke tcp handshake pertama -> klik kanan -> follow -> tcp stream -> show as ascii
12. kalo langsung nc nanti jadi
![[Pasted image 20260916161812.png]]
```
root@Alice:~# nc -zv 192.226.3.2 22
nc: connect to 192.226.3.2 port 22 (tcp) failed: Connection refused
root@Alice:~# nc -zv 192.226.3.2 80
nc: connect to 192.226.3.2 port 80 (tcp) failed: Connection refused
root@Alice:~# nc -zv 192.226.3.2 7777
nc: connect to 192.226.3.2 port 7777 (tcp) failed: Connection refused
```
- coba install dulu openssh-server dan nginx buat ssh dan http
> atau program apapun yang jalan di 80 dan 22

```
  root@Knights:~# ss -lntp | grep -E ':22|:80'
LISTEN 0      1            0.0.0.0:22        0.0.0.0:*    users:(("nc",pid=423,fd=3))
LISTEN 0      1            0.0.0.0:80        0.0.0.0:*    users:(("nc",pid=418,fd=3))
```

![[Pasted image 20260916164712.png]]
di port 80 dan 22 itu masing masing syn ack, ack, fin ack, ack fin ack, ack
kalo 7777 cuman syn, rst ack sepihak

13. somehow gabisa install openssh (sebelumnya buat install yang lain bisa) -> `apt -o Acquire::ForceIPv4=true update` -> `apt -o Acquire::ForceIPv4=true install openssh-server`
di knight
- normalnya apt update apt && install openssh-server -y
- service ssh start && service ssh status
- cek port ``ss -lnt | grep ':22'``
-  `useradd -m -s /bin/bash mika_admin`
di mika:
- `apt install openssh-client -y`
- `useradd -m -s /bin/bash mika_admin`
- masuk ke user mika_admin `su - mika_admin`
- bikin ssh key ssh-keygen -t ed25519
![[Pasted image 20260916183532.png]]
- copy pub key ke mika_admin nya knight
di knight:
- mkdir .ssh -> chmod 700 .ssh nya
- masukin pub key ke suatu file (authorized_keys)
- chmod 600 ~/.ssh/authorized_keys
di mika:
- ssh mika_admin@192.226.3.2
pov wireshark mika:
setelah handshake -> protocol version exchange client ke server, ack, server ke client, ack
![[Pasted image 20260916184313.png]]
lalu exchange key dan enkripsi
- client: key exchange init -> server:key exchange init
- bikin shared secret/session keys (yang PQ/T hybrid key)
- server kasih new key buat session itu -> client acc (`Client: New Keys, Encrypted packet`)
![[Pasted image 20260916184440.png]]
- selanjutnya jalan di enkripsi
![[Pasted image 20260916184940.png]]

Karena SSH mengenkripsi komunikasi antara client dan server, kredensial dan data tidak dikirim dalam bentuk plaintext seperti pada Telnet sehingga tidak dapat dibaca langsung melalui Wireshark.